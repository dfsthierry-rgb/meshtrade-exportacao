/* Central Mesh Export Flow - guided operational interface */
let currentStep=1;
S.currency='USD';
S.notifyParty ??= client().notify || 'Same as consignee';
S.generalNotes ??= '';

document.head.insertAdjacentHTML('beforeend',`<style>
:root{--black:#161819;--graphite:#25292b;--paper:#f7f5ef;--white:#fff;--yellow:#ffe500;--blue:#2563eb;--green:#167653;--red:#b43535;--stroke:#dedbd2;--soft:#ece9e1;--text:#222628;--muted:#6e7475}
body{background:var(--paper);color:var(--text);font-family:Inter,Arial,sans-serif}
.shell{min-height:100vh}.apphead{height:82px;background:var(--black);display:flex;align-items:center;justify-content:space-between;padding:0 max(28px,calc((100vw - 1320px)/2));color:white}.appbrand{display:flex;align-items:center;gap:18px}.appbrand img{width:165px;height:58px;object-fit:contain;background:#fff;border-radius:4px;padding:4px}.appbrand div{border-left:1px solid #565a5b;padding-left:18px}.appbrand b{display:block;font-size:14px;letter-spacing:.06em}.appbrand small{color:#aeb4b5}.headbuttons{display:flex;gap:8px}.headbuttons button{color:white;border:1px solid #575c5e;background:transparent}.workspace{max-width:1320px;margin:auto;padding:34px 28px 60px}.flowhead{display:flex;justify-content:space-between;align-items:flex-end;margin-bottom:25px}.flowhead h1{font-size:32px;margin:5px 0 0;letter-spacing:-.03em}.flowhead p{margin:0;color:var(--muted)}.kicker{font-size:10px;letter-spacing:.16em;font-weight:800;color:#866f00}.flow{display:grid;grid-template-columns:repeat(4,1fr);border:1px solid var(--stroke);background:#fff;margin-bottom:24px}.flowstep{position:relative;padding:15px 16px 14px 48px;border-right:1px solid var(--stroke);color:#7b8081}.flowstep:last-child{border:0}.flowstep .n{position:absolute;left:15px;top:14px;width:24px;height:24px;border-radius:50%;display:grid;place-items:center;background:var(--soft);font-size:11px;font-weight:900}.flowstep b{display:block;color:inherit;margin-bottom:2px}.flowstep.active{background:#fffbea;color:var(--black)}.flowstep.active .n{background:var(--yellow);color:#111}.flowstep.done{color:var(--green)}.flowstep.done .n{background:var(--green);color:#fff}.stage{display:none}.stage.active{display:block}.panel{background:#fff;border:1px solid var(--stroke);padding:24px}.panelhead{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:20px}.panelhead h2{margin:0 0 5px;font-size:21px}.muted{color:var(--muted);line-height:1.5}.upload{display:grid;place-items:center;min-height:280px;border:2px dashed #bdb9ae;background:#fbfaf6;text-align:center;padding:32px;transition:.2s}.upload:hover{border-color:#968500;background:#fffde8}.uploadmark{width:54px;height:54px;border-radius:50%;display:grid;place-items:center;background:var(--yellow);font-size:25px;margin-bottom:14px}.upload h3{font-size:19px;margin:0 0 6px}.upload input{max-width:300px}.twocol{display:grid;grid-template-columns:minmax(0,1.6fr) 360px;gap:18px}.sidecard{background:var(--graphite);color:#fff;padding:22px;position:sticky;top:18px;height:max-content}.sidecard h3{margin:0 0 18px}.money{font-size:32px;font-weight:900;letter-spacing:-.04em;margin:8px 0 18px}.sidecard label{color:#c8cdce}.sidecard input{background:#fff;color:#111;font-size:20px;font-weight:800;padding:12px}.ratehint{font-size:11px;color:#aeb4b5;margin-top:6px}.kv{display:flex;justify-content:space-between;border-top:1px solid #4a4e50;padding:11px 0}.kv span{color:#bdc2c3}.stageactions{display:flex;justify-content:flex-end;gap:9px;margin-top:18px}.maincta{background:var(--yellow);color:#111;border:0;padding:13px 19px;border-radius:6px;font-weight:900;cursor:pointer}.backcta{background:#fff;border:1px solid var(--stroke);padding:12px 17px;border-radius:6px;font-weight:700;cursor:pointer}.orderblock{border:1px solid var(--stroke);margin-bottom:16px}.ordertitle{display:flex;gap:20px;align-items:center;padding:12px 14px;background:#f2f0e9}.ordertitle strong{font-size:15px}.ordertitle span{color:var(--muted);font-size:12px}.cleanTable th{background:#292d2f;color:white}.cleanTable td{background:#fff}.cleanTable input,.cleanTable textarea{background:#fffef7;border:1px solid #d9d4c8}.warning{padding:12px;border-left:4px solid var(--yellow);background:#fffbe5;margin:12px 0}.success{padding:12px;border-left:4px solid var(--green);background:#edf8f3}.logistics-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.logrow{border:1px solid var(--stroke);padding:16px;margin-bottom:12px;background:#fff}.logrowhead{display:flex;justify-content:space-between;margin-bottom:12px}.settingsDrawer{display:none;position:fixed;inset:0;z-index:20;background:#0008}.settingsDrawer.open{display:block}.settingsbox{position:absolute;right:0;top:0;bottom:0;width:min(680px,100%);background:var(--paper);padding:28px;overflow:auto}.settingsbox h2{margin-top:0}.formgrid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.formgrid .wide{grid-column:1/-1}.docswitch{display:flex;gap:5px;margin-bottom:10px}.docswitch button.active{background:var(--black);color:white}.documentPane{display:none}.documentPane.active{display:block}.paper{border-color:#c9c5ba}.editbar{background:#f1efe8;border-color:#d5d1c6}.docedit{background:#fffdf3}.badge{display:inline-block;background:#e6f2ee;color:#126a4a;border-radius:20px;padding:5px 9px;font-size:11px;font-weight:800}.invoiceNo{font-family:ui-monospace,Consolas,monospace}.errorbox{color:var(--red);font-weight:700;margin-top:10px}@media(max-width:900px){.apphead{height:auto;padding:14px}.appbrand div{display:none}.workspace{padding:20px 14px}.flow{grid-template-columns:1fr 1fr}.twocol{grid-template-columns:1fr}.sidecard{position:static}.logistics-grid,.formgrid{grid-template-columns:1fr}.flowhead{align-items:flex-start;gap:10px}.appbrand img{width:130px}.headbuttons{flex-wrap:wrap}}
</style>`);
document.head.insertAdjacentHTML('beforeend',`<style>.clientpick{display:flex;align-items:end;gap:10px;background:#f1efe8;border:1px solid var(--stroke);padding:15px;margin-bottom:16px}.clientpick label{flex:1}.clientpick select{font-size:16px;font-weight:800}.clientlist{display:grid;gap:8px;margin:14px 0}.clientcard{display:flex;justify-content:space-between;align-items:center;border:1px solid var(--stroke);background:#fff;padding:12px}.clientcard.active{border-left:5px solid var(--yellow)}@media(max-width:900px){.clientpick{display:grid}}</style>`);

document.body.innerHTML=`<div class="shell">
<header class="apphead"><div class="appbrand"><img src="central-mesh-logo.png"><div><b>EXPORT DOCUMENT STUDIO</b><small>Central Mesh · Commercial Invoice & Packing List</small></div></div><div class="headbuttons"><button class="btn" onclick="openDashboard()">Dashboard</button><button class="btn" onclick="newOperation()">Nova operação</button><button class="btn" onclick="openClients()">Clientes / Consignees</button><button class="btn" onclick="openSettings()">Central Mesh</button></div></header>
<main class="workspace"><div class="flowhead"><div><div class="kicker">OPERAÇÃO DE EXPORTAÇÃO</div><h1 id="flowTitle">Importar pedidos</h1></div><p id="flowSubtitle">Comece pelos pedidos de venda do cliente.</p></div>
<div class="flow" id="flowNav"></div>
<section class="stage active" id="stage1"><div class="panel"><div class="panelhead"><div><h2>Pedidos de venda</h2><div class="muted">Primeiro selecione o cliente/consignee. Depois importe um ou mais PDFs para consolidar a operação.</div></div><span class="badge">PDF</span></div><div class="clientpick"><label>Cliente / Consignee selecionado<select id="wizardClient" onchange="selectWizardClient(this.value)"></select></label><button class="backcta" onclick="createClient()">+ Cadastrar novo</button><button class="backcta" onclick="openClients()">Abrir arquivo de clientes</button></div><label class="upload"><div><div class="uploadmark">↑</div><h3>Selecione os pedidos</h3><div class="muted">Ex.: Pedido165449_MIRGOR_SACIFIA.pdf</div><input type="file" accept="application/pdf" multiple onchange="wizardImport(this.files)"></div></label><div id="uploadedSummary"></div></div></section>
<section class="stage" id="stage2"><div class="twocol"><div class="panel"><div class="panelhead"><div><h2>Revisar dados da operação</h2><div class="muted">Corrija qualquer informação que não tenha sido reconhecida no PDF.</div></div><span id="reviewBadge" class="badge"></span></div><div id="reviewOrders"></div></div><aside class="sidecard"><h3>Conversão para USD</h3><label>Câmbio BRL por USD<input id="wizardFx" type="number" step=".0001" oninput="updateFx(this.value)" onchange="renderReview()" placeholder="0,0000"></label><div class="ratehint">Taxa fornecida pela comissária.</div><div class="kv"><span>Total dos pedidos</span><b id="brlTotal"></b></div><div class="kv"><span>Total convertido</span><b id="usdTotalSide"></b></div><div class="kv"><span>Invoice</span><b id="invoiceNumberSide" class="invoiceNo"></b></div><div class="stageactions"><button class="maincta" onclick="processReview()">Confirmar e continuar →</button></div><div id="reviewError" class="errorbox"></div></aside></div></section>
<section class="stage" id="stage3"><div class="panel"><div class="panelhead"><div><h2>Expedição e Logística</h2><div class="muted">Informe a composição física do embarque. Os totais serão validados antes da emissão.</div></div><button class="backcta" onclick="addWizardPack()">+ Adicionar embalagem</button></div><div id="logisticsRows"></div><div class="warning">Informe caixas, peças por caixa, pacotes, peso líquido, peso bruto, dimensões, marcas e observações conforme os dados da Expedição.</div><div class="stageactions"><button class="backcta" onclick="goStep(2)">← Voltar</button><button class="maincta" onclick="processDocuments()">Processar documentos →</button></div><div id="logisticsError" class="errorbox"></div></div></section>
<section class="stage" id="stage4"><div class="success"><b>Documentos processados.</b> Invoice e Packing List estão conectadas e prontas para revisão final.</div><div class="stageactions"><button class="backcta" onclick="goStep(3)">Editar logística</button><button class="backcta" onclick="downloadExcel()">Baixar Excel editável</button><button class="maincta" onclick="downloadPDF()">Baixar PDF oficial</button></div><div class="docswitch"><button class="btn light active" onclick="switchDoc('invoice',this)">Commercial Invoice</button><button class="btn light" onclick="switchDoc('packing',this)">Packing List</button></div><div id="invoiceView" class="documentPane active"></div><div id="packingView" class="documentPane"></div></section></main>
<div class="settingsDrawer" id="settingsDrawer"><div class="settingsbox"><div class="panelhead"><div><h2>Central Mesh</h2><div class="muted">Dados fixos do exportador, banco e identificação fiscal.</div></div><button class="backcta" onclick="closeSettings()">Fechar</button></div><div id="exporterSettings" class="formgrid"></div><div class="stageactions"><button class="maincta" onclick="save(false);closeSettings()">Salvar dados da Central Mesh</button></div></div></div>
<div class="settingsDrawer" id="clientsDrawer"><div class="settingsbox"><div class="panelhead"><div><h2>Clientes / Consignees</h2><div class="muted">Arquivo permanente de importadores disponíveis para novas operações.</div></div><button class="backcta" onclick="closeClients()">Fechar</button></div><button class="maincta" onclick="createClient()">+ Novo cliente</button><div id="clientList" class="clientlist"></div><h3>Cadastro selecionado</h3><div id="clientSettings" class="formgrid"></div><div class="stageactions"><button class="maincta" onclick="quietSave();closeClients();renderWizard()">Salvar cliente</button></div></div></div>
<div class="settingsDrawer" id="dashboardDrawer"><div class="settingsbox" style="width:min(1080px,100%)"><div class="panelhead"><div><h2>Dashboard de Invoices</h2><div class="muted">Histórico de documentos processados neste dispositivo.</div></div><button class="backcta" onclick="closeDashboard()">Fechar</button></div><div id="dashboardMetrics" class="summary"></div><label>Pesquisar por Invoice, cliente ou pedido<input id="dashboardSearch" oninput="renderDashboard(this.value)" placeholder="Digite para pesquisar"></label><div id="dashboardTable" style="margin-top:15px"></div></div></div></div>`;

function nextInvoiceNumber(){
  if(S.invoiceNo)return;
  const year=new Date().getFullYear(),key='cm-invoice-sequence-'+year;
  const seq=(Number(localStorage.getItem(key)||0)+1);
  localStorage.setItem(key,seq);
  S.invoiceNo='CM-'+year+'-'+String(seq).padStart(4,'0');
  S.invoiceDate=new Date().toISOString().slice(0,10);
}
function goStep(step){
  if(step>=2&&S.orders.length)nextInvoiceNumber();
  currentStep=step;
  document.querySelectorAll('.stage').forEach((x,i)=>x.classList.toggle('active',i===step-1));
  const titles=[['Importar pedidos','Comece pelos pedidos de venda do cliente.'],['Revisar e converter','Confirme os itens e aplique o câmbio da comissária.'],['Expedição e Logística','Complete a composição física do embarque.'],['Documentos finais','Revise, edite e baixe os documentos oficiais.']];
  flowTitle.textContent=titles[step-1][0];flowSubtitle.textContent=titles[step-1][1];renderFlow();renderWizard();
}
function renderFlow(){const names=['Pedidos','Câmbio e revisão','Expedição','Documentos'];flowNav.innerHTML=names.map((n,i)=>`<div class="flowstep ${i+1===currentStep?'active':i+1<currentStep?'done':''}"><span class="n">${i+1<currentStep?'✓':i+1}</span><b>${n}</b><small>${['Importar PDFs','Confirmar valores','Pesos e volumes','Invoice + Packing'][i]}</small></div>`).join('')}
async function wizardImport(files){
  if(!files.length)return;
  nextInvoiceNumber();
  for(const f of files){try{const o=parseOrder(await pdfLines(f),f);S.orders.push(o)}catch(err){alert('Não foi possível ler '+f.name)}}
  quietSave();goStep(2);
}
function operationBRL(){return allItems().reduce((a,x)=>a+(+x.qty||0)*(+x.unitBRL||0),0)}
function updateFx(v){S.fx=Number(v);quietSave();updateTotals()}
function updateTotals(){if(!document.getElementById('brlTotal'))return;brlTotal.textContent=new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(operationBRL());usdTotalSide.textContent=+S.fx?usd(operationBRL()/S.fx):'Aguardando câmbio';invoiceNumberSide.textContent=S.invoiceNo||'—'}
function renderReview(){
  reviewBadge.textContent=S.orders.length+' pedido(s) · '+allItems().length+' item(ns)';
  wizardFx.value=S.fx||'';
  reviewOrders.innerHTML=S.orders.map((o,oi)=>`<div class="orderblock"><div class="ordertitle"><strong>Pedido ${esc(o.order||'não identificado')}</strong><span>PO ${esc(o.customerPO||'não identificado')}</span><span>Entrega ${esc(o.delivery||'—')}</span><button class="btn light mini danger" onclick="removeWizardOrder(${oi})">Excluir</button></div><table class="cleanTable"><tr><th>Código</th><th>Descrição completa em inglês</th><th>HS/NCM</th><th>Qtd.</th><th>Unit. BRL</th><th>Unit. USD</th></tr>${o.items.map((x,ii)=>`<tr><td><input value="${esc(x.code)}" onchange="S.orders[${oi}].items[${ii}].code=this.value;quietSave()"></td><td><textarea onchange="S.orders[${oi}].items[${ii}].description=this.value;quietSave()">${esc(x.description)}</textarea></td><td><input value="${esc(x.hs)}" onchange="S.orders[${oi}].items[${ii}].hs=this.value;quietSave()"></td><td><input type="number" value="${x.qty}" onchange="S.orders[${oi}].items[${ii}].qty=+this.value;quietSave();updateTotals()"></td><td><input type="number" step=".01" value="${x.unitBRL}" onchange="S.orders[${oi}].items[${ii}].unitBRL=+this.value;quietSave();updateTotals()"></td><td>${+S.fx?usd(x.unitBRL/S.fx):'—'}</td></tr>`).join('')}</table></div>`).join('');
  updateTotals();
}
function removeWizardOrder(i){S.orders.splice(i,1);quietSave();renderWizard();if(!S.orders.length)goStep(1)}
function processReview(){reviewError.textContent='';if(!S.orders.length)return reviewError.textContent='Importe pelo menos um pedido.';if(!+S.fx)return reviewError.textContent='Informe o câmbio fornecido pela comissária.';if(!S.packs.length)addWizardPack(false);quietSave();goStep(3)}
function addWizardPack(draw=true){S.packs.push({box:S.packs.length+1,cartons:1,piecesPerCarton:'',packages:1,description:'',length:'',width:'',height:'',net:'',gross:'',marks:'CENTRAL MESH / '+client().name,notes:''});if(draw)renderLogistics()}
function setPack(i,k,v){S.packs[i][k]=['description','marks','notes'].includes(k)?v:Number(v);quietSave();renderLogTotals()}
function renderLogistics(){
  logisticsRows.innerHTML=S.packs.map((x,i)=>`<div class="logrow"><div class="logrowhead"><strong>Embalagem ${i+1}</strong><button class="btn light mini danger" onclick="S.packs.splice(${i},1);renderLogistics()">Excluir</button></div><div class="logistics-grid"><label>Quantidade de caixas<input type="number" value="${esc(x.cartons)}" onchange="setPack(${i},'cartons',this.value)"></label><label>Peças por caixa<input type="number" value="${esc(x.piecesPerCarton)}" onchange="setPack(${i},'piecesPerCarton',this.value)"></label><label>Pacotes<input type="number" value="${esc(x.packages)}" onchange="setPack(${i},'packages',this.value)"></label><label>Quantidade total<input type="number" value="${esc(x.qty)}" onchange="setPack(${i},'qty',this.value)"></label><label>Comprimento cm<input type="number" step=".01" value="${esc(x.length)}" onchange="setPack(${i},'length',this.value)"></label><label>Largura cm<input type="number" step=".01" value="${esc(x.width)}" onchange="setPack(${i},'width',this.value)"></label><label>Altura cm<input type="number" step=".01" value="${esc(x.height)}" onchange="setPack(${i},'height',this.value)"></label><label>Peso líquido kg<input type="number" step=".001" value="${esc(x.net)}" onchange="setPack(${i},'net',this.value)"></label><label>Peso bruto kg<input type="number" step=".001" value="${esc(x.gross)}" onchange="setPack(${i},'gross',this.value)"></label><label class="full">Marcas / identificação<input value="${esc(x.marks)}" onchange="setPack(${i},'marks',this.value)"></label><label class="full">Descrição vinculada à Invoice<textarea placeholder="Em branco: usa as descrições da Invoice" onchange="setPack(${i},'description',this.value)">${esc(x.description)}</textarea></label><label class="full">Observações da Expedição<textarea onchange="setPack(${i},'notes',this.value)">${esc(x.notes)}</textarea></label></div></div>`).join('')+'<div id="logTotals" class="summary"></div>';renderLogTotals()
}
function renderLogTotals(){if(!document.getElementById('logTotals'))return;const cartons=S.packs.reduce((a,x)=>a+(+x.cartons||0),0),packages=S.packs.reduce((a,x)=>a+(+x.packages||0),0),net=S.packs.reduce((a,x)=>a+(+x.net||0),0),gross=S.packs.reduce((a,x)=>a+(+x.gross||0),0);logTotals.innerHTML=`<div class="metric"><small>Caixas</small><b>${cartons}</b></div><div class="metric"><small>Pacotes</small><b>${packages}</b></div><div class="metric"><small>Peso líquido</small><b>${net.toFixed(3)} kg</b></div><div class="metric"><small>Peso bruto</small><b>${gross.toFixed(3)} kg</b></div>`}
function processDocuments(){
  logisticsError.textContent='';
  if(!S.packs.length)return logisticsError.textContent='Adicione pelo menos uma embalagem.';
  if(S.packs.some(x=>!(+x.cartons)||!(+x.packages)||!(+x.net)||!(+x.gross)))return logisticsError.textContent='Complete caixas, pacotes, peso líquido e peso bruto.';
  if(S.packs.some(x=>+x.gross<+x.net))return logisticsError.textContent='O peso bruto não pode ser menor que o peso líquido.';
  quietSave();saveInvoiceHistory();goStep(4);
}
function history(){return JSON.parse(localStorage.getItem('cm-invoice-history')||'[]')}
function saveInvoiceHistory(){
  const records=history(),snapshot=JSON.parse(JSON.stringify(S)),existing=records.findIndex(x=>x.invoiceNo===S.invoiceNo);
  const record={invoiceNo:S.invoiceNo,date:S.invoiceDate,customer:client().name,orders:S.orders.map(o=>o.customerPO||o.order).join(', '),totalUSD:usdTotal()+(+S.freightUSD||0)+(+S.insuranceUSD||0)+(+S.otherUSD||0),cartons:S.packs.reduce((a,x)=>a+(+x.cartons||0),0),packages:S.packs.reduce((a,x)=>a+(+x.packages||0),0),net:S.packs.reduce((a,x)=>a+(+x.net||0),0),gross:S.packs.reduce((a,x)=>a+(+x.gross||0),0),savedAt:new Date().toISOString(),data:snapshot};
  if(existing>=0)records[existing]=record;else records.unshift(record);
  localStorage.setItem('cm-invoice-history',JSON.stringify(records));
}
function openDashboard(){renderDashboard('');dashboardDrawer.classList.add('open')}function closeDashboard(){dashboardDrawer.classList.remove('open')}
function renderDashboard(query=''){
  const q=String(query).toLowerCase(),all=history(),rows=all.filter(x=>[x.invoiceNo,x.customer,x.orders].join(' ').toLowerCase().includes(q));
  dashboardMetrics.innerHTML=`<div class="metric"><small>Invoices emitidas</small><b>${all.length}</b></div><div class="metric"><small>Valor acumulado</small><b>${usd(all.reduce((a,x)=>a+(+x.totalUSD||0),0))}</b></div><div class="metric"><small>Clientes</small><b>${new Set(all.map(x=>x.customer)).size}</b></div><div class="metric"><small>Peso bruto acumulado</small><b>${all.reduce((a,x)=>a+(+x.gross||0),0).toFixed(3)} kg</b></div>`;
  dashboardTable.innerHTML=rows.length?`<table class="cleanTable"><tr><th>Invoice</th><th>Data</th><th>Consignee</th><th>PO / Pedidos</th><th>Total USD</th><th>Caixas / Pacotes</th><th>Peso líquido / bruto</th><th></th></tr>${rows.map(x=>`<tr><td class="invoiceNo"><b>${esc(x.invoiceNo)}</b></td><td>${esc(x.date)}</td><td>${esc(x.customer)}</td><td>${esc(x.orders)}</td><td>${usd(x.totalUSD)}</td><td>${x.cartons} / ${x.packages}</td><td>${(+x.net).toFixed(3)} / ${(+x.gross).toFixed(3)} kg</td><td><button class="backcta" onclick="openHistoryInvoice('${esc(x.invoiceNo)}')">Abrir</button></td></tr>`).join('')}</table>`:'<div class="empty">Nenhuma Invoice encontrada.</div>';
}
function openHistoryInvoice(number){const record=history().find(x=>x.invoiceNo===number);if(!record)return;S=JSON.parse(JSON.stringify(record.data));quietSave();closeDashboard();goStep(4)}
function switchDoc(kind,btn){document.querySelectorAll('.documentPane,.docswitch button').forEach(x=>x.classList.remove('active'));document.getElementById(kind+'View').classList.add('active');btn.classList.add('active')}
const renderDocsBase=renderDocs;
renderDocs=function(){
  renderDocsBase();
  const terms=`<div class="docmeta"><div>CURRENCY<strong>USD</strong></div><div>PAYMENT TERMS<strong>${esc(S.payment||'—')}</strong></div><div>TRANSPORT<strong>${esc(S.transport||'—')}</strong></div><div>ORIGIN / DESTINATION<strong>${esc(S.origin)} / ${esc(S.destination)}</strong></div></div><div class="docnote"><b>NOTIFY PARTY</b>${esc(S.notifyParty||client().notify||'Same as consignee')}</div>`;
  invoiceView.querySelector('.docmeta')?.insertAdjacentHTML('afterend',terms);
  packingView.querySelector('.docmeta')?.insertAdjacentHTML('afterend',terms);
  const detail=`<div class="docnote"><b>PACKAGING DETAILS</b><table><tr><th>Cartons</th><th>Pieces / Carton</th><th>Packages</th><th>Marks</th><th>Dispatch observations</th></tr>${S.packs.map(x=>`<tr><td>${esc(x.cartons)}</td><td>${esc(x.piecesPerCarton)}</td><td>${esc(x.packages)}</td><td>${esc(x.marks)}</td><td>${esc(x.notes)}</td></tr>`).join('')}</table></div>`;
  packingView.querySelector('.total')?.insertAdjacentHTML('beforebegin',detail);
};
function inputField(label,action,value,wide=false){return `<label class="${wide?'wide':''}">${label}<input value="${esc(value)}" onchange="${action}"></label>`}
function renderSettings(){exporterSettings.innerHTML=inputField('Razão social',"S.exporter.name=this.value;quietSave()",S.exporter.name,true)+inputField('CNPJ',"S.exporter.tax=this.value;quietSave()",S.exporter.tax)+inputField('Telefone',"S.exporter.phone=this.value;quietSave()",S.exporter.phone)+inputField('Endereço',"S.exporter.address=this.value;quietSave()",S.exporter.address,true)+inputField('E-mail',"S.exporter.email=this.value;quietSave()",S.exporter.email)+inputField('Banco beneficiário',"S.exporter.bank=this.value;quietSave()",S.exporter.bank)+inputField('SWIFT',"S.exporter.swift=this.value;quietSave()",S.exporter.swift)+inputField('Conta / IBAN',"S.exporter.account=this.value;quietSave()",S.exporter.account)}
function renderClients(){clientList.innerHTML=S.clients.map(c=>`<div class="clientcard ${c.id===S.activeClient?'active':''}"><div><b>${esc(c.name)}</b><div class="muted">${esc(c.country||'País não informado')} · ${esc(c.tax||'Tax ID não informado')}</div></div><button class="backcta" onclick="selectClientRecord('${c.id}')">Selecionar</button></div>`).join('');clientSettings.innerHTML=inputField('Consignee / Legal name',"client().name=this.value;quietSave();renderClientPicker()",client().name,true)+inputField('Tax ID',"client().tax=this.value;quietSave()",client().tax)+inputField('País',"client().country=this.value;quietSave()",client().country)+inputField('Endereço completo',"client().address=this.value;quietSave()",client().address,true)+inputField('Notify Party',"client().notify=this.value;S.notifyParty=this.value;quietSave()",client().notify||'',true)+inputField('Contato / Observações',"client().notes=this.value;quietSave()",client().notes||'',true)}
function renderClientPicker(){if(!document.getElementById('wizardClient'))return;wizardClient.innerHTML=S.clients.map(c=>`<option value="${c.id}" ${c.id===S.activeClient?'selected':''}>${esc(c.name)}</option>`).join('')}
function selectWizardClient(id){S.activeClient=id;S.notifyParty=client().notify||'Same as consignee';quietSave()}
function selectClientRecord(id){selectWizardClient(id);renderClients();renderClientPicker()}
function createClient(){const c={id:'client-'+Date.now(),name:'Novo cliente',tax:'',address:'',country:'',notify:'Same as consignee',notes:''};S.clients.push(c);S.activeClient=c.id;quietSave();openClients()}
function openSettings(){renderSettings();settingsDrawer.classList.add('open')}function closeSettings(){settingsDrawer.classList.remove('open')}
function openClients(){renderClients();clientsDrawer.classList.add('open')}function closeClients(){clientsDrawer.classList.remove('open')}
function newOperation(){if(S.orders.length&&!confirm('Iniciar uma nova operação? Os dados da operação atual serão removidos.'))return;const fixed={exporter:S.exporter,clients:S.clients,activeClient:S.activeClient};S=Object.assign(defaults(),fixed);currentStep=1;quietSave();goStep(1)}
downloadExcel=async function(){
  if(!S.orders.length)return alert('Importe um pedido primeiro.');
  const wb=new ExcelJS.Workbook(),brand=wb.addImage({base64:await logo(),extension:'png'}),po=S.poDisplay||S.orders.map(o=>o.customerPO||o.order).join(', ');
  const setup=(name,cols)=>{
    const ws=wb.addWorksheet(name,{pageSetup:{paperSize:9,orientation:'portrait',fitToPage:true,fitToWidth:1,fitToHeight:1,margins:{left:.25,right:.25,top:.35,bottom:.35,header:.1,footer:.1}}});
    ws.views=[{showGridLines:false}];ws.columns=cols.map(w=>({width:w}));ws.addImage(brand,{tl:{col:.15,row:.2},ext:{width:170,height:62}});
    ws.mergeCells(1,1,3,cols.length);let t=ws.getCell(1,1);t.value=name.toUpperCase();t.font={bold:true,size:18,color:{argb:'FF25292B'}};t.alignment={horizontal:'right',vertical:'middle'};
    ws.mergeCells(5,1,5,Math.floor(cols.length/2));ws.getCell(5,1).value='SHIPPER';ws.mergeCells(5,Math.floor(cols.length/2)+1,5,cols.length);ws.getCell(5,Math.floor(cols.length/2)+1).value='CONSIGNEE';
    ws.mergeCells(6,1,8,Math.floor(cols.length/2));ws.getCell(6,1).value=S.exporter.name+'\n'+S.exporter.address+'\nTax ID: '+S.exporter.tax;
    ws.mergeCells(6,Math.floor(cols.length/2)+1,8,cols.length);ws.getCell(6,Math.floor(cols.length/2)+1).value=client().name+'\n'+client().address+'\nTax ID: '+client().tax;
    ws.addRow([]);ws.addRow(['Invoice / Packing No.',S.invoiceNo,'Issue Date',S.invoiceDate,'PO',po,'Incoterm',S.incoterm]);ws.addRow(['Currency','USD','Payment Terms',S.payment,'Transport',S.transport,'Origin / Destination',S.origin+' / '+S.destination]);ws.addRow(['Notify Party',S.notifyParty]);
    ws.getRow(5).font={bold:true,color:{argb:'FFFFFFFF'}};ws.getRow(5).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF25292B'}};
    return ws;
  };
  const ci=setup('Commercial Invoice',[7,15,35,3,3,14,10,15,16]);
  ci.addRow([]);ci.addRow(['Item','NCM/HS Code','Description','','','Net Weight (kg)','Qty.','Unit Value (USD)','Total Value (USD)']);
  const head=ci.rowCount;allItems().forEach((x,i)=>ci.addRow([i+1,x.hs,x.description,'','',+x.netWeight||'',+x.qty||0,+x.unitBRL/S.fx,{formula:`G${ci.rowCount+1}*H${ci.rowCount+1}`}]));
  ci.addRow(['','','','','','','','Goods Value',{formula:`SUM(I${head+1}:I${ci.rowCount})`}]);ci.addRow(['','','','','','','','Freight',+S.freightUSD||0]);ci.addRow(['','','','','','','','Insurance',+S.insuranceUSD||0]);ci.addRow(['','','','','','','','Other Charges',+S.otherUSD||0]);ci.addRow(['','','','','','','','TOTAL',{formula:`SUM(I${ci.rowCount-3}:I${ci.rowCount})`}]);ci.addRow(['Declaration / Notes',S.declaration]);ci.addRow(['Bank / SWIFT / Account',[S.exporter.bank,S.exporter.swift,S.exporter.account].filter(Boolean).join(' · ')]);
  const pl=setup('Packing List',[9,25,9,10,10,10,10,12,12,12,20]);pl.addRow([]);pl.addRow(['Box','Description','Qty.','Cartons','Pieces / Carton','Packages','Length cm','Width cm','Height cm','Net kg','Gross kg']);
  S.packs.forEach(x=>pl.addRow([x.box,x.description||connectedDescription(),+x.qty||(+x.cartons*+x.piecesPerCarton)||0,+x.cartons||0,+x.piecesPerCarton||0,+x.packages||0,+x.length||0,+x.width||0,+x.height||0,+x.net||0,+x.gross||0]));
  pl.addRow(['Marks / Notes',S.packs.map(x=>[x.marks,x.notes].filter(Boolean).join(': ')).join(' | ')]);
  for(const ws of wb.worksheets){const hr=ws.getRow(14);hr.font={bold:true,color:{argb:'FFFFFFFF'}};hr.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF25292B'}};ws.eachRow(r=>{r.alignment={vertical:'top',wrapText:true};r.eachCell(c=>c.border={bottom:{style:'thin',color:{argb:'FFD8D5CD'}}})});ws.getColumn(8).numFmt='"$"#,##0.00';ws.getColumn(9).numFmt='"$"#,##0.00'}
  const buf=await wb.xlsx.writeBuffer(),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([buf]));a.download=(S.invoiceNo||'Central-Mesh-Export')+'.xlsx';a.click();
};
downloadPDF=async function(){
  if(!S.orders.length)return alert('Importe um pedido primeiro.');
  const{jsPDF}=window.jspdf,d=new jsPDF(),ld=await logo(),po=S.poDisplay||S.orders.map(o=>o.customerPO||o.order).join(', ');
  const header=title=>{d.addImage(ld,'PNG',12,7,60,22);d.setFontSize(17);d.text(title,198,18,{align:'right'});d.setDrawColor(40);d.line(12,32,198,32);d.setFontSize(8);d.text('SHIPPER\n'+S.exporter.name+'\n'+S.exporter.address+'\n'+S.exporter.tax,12,39,{maxWidth:86});d.text('CONSIGNEE\n'+client().name+'\n'+client().address+'\n'+client().tax,108,39,{maxWidth:90});d.text('No. '+S.invoiceNo+'   Date '+S.invoiceDate+'   PO '+po,12,62);d.text('Incoterm '+S.incoterm+'   Currency USD   Transport '+S.transport,12,68);d.text('Origin '+S.origin+'   Destination '+S.destination+'   Payment '+S.payment,12,74)};
  header('COMMERCIAL INVOICE');d.autoTable({startY:79,head:[['Item','HS/NCM','Description','Net kg','Qty','Unit USD','Total USD']],body:allItems().map((x,i)=>[i+1,x.hs,x.description,x.netWeight,x.qty,usd(x.unitBRL/S.fx),usd(x.qty*x.unitBRL/S.fx)]),styles:{fontSize:7},headStyles:{fillColor:[37,41,43]}});let y=d.lastAutoTable.finalY+9;d.text('Goods '+usd(usdTotal())+'   Freight '+usd(S.freightUSD)+'   Insurance '+usd(S.insuranceUSD)+'   Other '+usd(S.otherUSD),198,y,{align:'right'});d.setFont(undefined,'bold');d.text('TOTAL '+usd(usdTotal()+(+S.freightUSD||0)+(+S.insuranceUSD||0)+(+S.otherUSD||0)),198,y+7,{align:'right'});d.setFont(undefined,'normal');d.text('Notify Party: '+S.notifyParty,12,y+16);d.text('Declaration: '+S.declaration,12,y+23,{maxWidth:186});
  d.addPage();header('PACKING LIST');d.autoTable({startY:79,head:[['Box','Description','Qty','Cartons','Pc/Carton','Packages','L×W×H cm','Net kg','Gross kg']],body:S.packs.map(x=>[x.box,x.description||connectedDescription(),x.qty||(+x.cartons*+x.piecesPerCarton),x.cartons,x.piecesPerCarton,x.packages,x.length+'×'+x.width+'×'+x.height,x.net,x.gross]),styles:{fontSize:6.8},headStyles:{fillColor:[37,41,43]}});y=d.lastAutoTable.finalY+9;d.text('Marks / Notes: '+S.packs.map(x=>[x.marks,x.notes].filter(Boolean).join(' - ')).join(' | '),12,y,{maxWidth:186});d.text('TOTAL PACKAGES '+S.packs.reduce((a,x)=>a+(+x.packages||0),0)+'   NET '+S.packs.reduce((a,x)=>a+(+x.net||0),0).toFixed(3)+' kg   GROSS '+S.packs.reduce((a,x)=>a+(+x.gross||0),0).toFixed(3)+' kg',198,y+13,{align:'right'});d.save((S.invoiceNo||'Central-Mesh-Export')+'.pdf');
};
function renderWizard(){renderFlow();renderClientPicker();if(currentStep===1){uploadedSummary.innerHTML=S.orders.length?'<div class="success" style="margin-top:14px">'+S.orders.length+' pedido(s) já importado(s). <button class="linkbtn" onclick="goStep(2)">Continuar revisão</button></div>':''}if(currentStep===2)renderReview();if(currentStep===3)renderLogistics();if(currentStep===4)renderDocs()}
S.orders.forEach(o=>{if(!o.order)o.order=(String(o.file||'').match(/(\d{6})/)||[])[1]||''});
if(S.orders.length)quietSave();
renderFlow();renderWizard();
